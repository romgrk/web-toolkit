import React from 'react';
import ReactDOM from 'react-dom';
import 'web-toolkit-theme-adwaita';
import './index.css';
import App from './components/App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
